# Whatsapp Numbers Checker

أداة بسيطة وفعالة لفحص الأرقام ومعرفة ما إذا كانت مسجلة في واتساب أم لا.

## 🚀 طريقة الاستخدام (للمستخدم العادي)

1. **شراء المفتاح:** قم بشراء مفتاح API من الموقع الرسمي: [https://wasenderapi.com](https://wasenderapi.com)
2. **إعداد المفتاح:** تأكد من وجود ملف `api.txt` بجانب البرنامج، وضع بداخله مفتاح API الذي حصلت عليه.
3. **إعداد الأرقام:** ضع الأرقام التي تريد فحصها في ملف `numbers.txt` (كل رقم في سطر).
4. **تشغيل البرنامج:** قم بتشغيل الملف التنفيذي `WhatsappChecker.exe`.
5. **النتائج:** سيقوم البرنامج بإنشاء ملفين:
    - `valid.txt`: يحتوي على الأرقام الصحيحة (المسجلة في واتساب).
    - `invalid.txt`: يحتوي على الأرقام غير المسجلة.

## 🛠️ للمطورين (تشغيل السكريبت)

تأكد من تثبيت المكتبات المطلوبة:
```bash
pip install -r requirements.txt
```
ثم شغل السكريبت:
```bash
python main.py
```

## ⚠️ ملاحظات
- البرنامج يتطلب اتصال بالإنترنت.
- تأكد من عدم وجود مسافات فارغة في ملف `api.txt`.

---
**حقوق التطوير والملكية:**
© شركة الصقر للبرمجيات (Elsakr Soft)
الموقع الرسمي: [https://elsakr.company](https://elsakr.company)
